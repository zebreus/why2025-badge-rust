== Hello World Example

Minimal std example for using rust on the WHY2025 badge.

This example targets `riscv32imafc-unknown-badgevms` using our badgevms toolchain.

Build it with:

[source,sh]
----
cargo +badgevms build --target riscv32imafc-unknown-badgevms
----